---
// Navigation.astro - Tüm sayfalarda kullanılacak ortak navigasyon bileşeni
---

<div class="nav-container">
  <div class="border-wrapper full-width">
    <div class="nav-border nav-border-top"></div>
  </div>
  <nav class="main-nav">
    <a href="/" class="nav-item reset-link">Ana Sayfa</a>
    <a href="/hakkinda" class="nav-item reset-link">Hakkımızda</a>
  </nav>
  <div class="border-wrapper full-width">
    <div class="nav-border nav-border-bottom"></div>
  </div>
</div>

<style>
  /* Tüm bağlantı stillerini sıfırla */
  .reset-link {
    text-decoration: none !important;
    border-bottom: none !important;
    background-image: none !important;
    box-shadow: none !important;
  }
  
  .reset-link::after,
  .reset-link::before {
    display: none !important;
    content: none !important;
  }
  
  .nav-container {
    position: relative;
    width: 100%;
    margin-bottom: 30px;
    padding: 0;
    z-index: 10;
    height: 80px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  
  .border-wrapper {
    position: relative;
    width: 100%;
    height: 1px;
    overflow: hidden;
  }
  
  .full-width {
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;
    width: 100vw;
    position: relative;
  }
  
  .main-nav {
    display: flex;
    gap: 10px;
    align-items: center;
    margin: 10px 0;
    height: 40px;
  }
  
  .nav-border {
    position: absolute;
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, 
      rgba(0,0,0,0) 0%, 
      rgba(0,102,204,0.2) 15%, 
      rgba(0,102,204,0.5) 30%, 
      rgba(0,102,204,1) 50%, 
      rgba(0,102,204,0.5) 70%, 
      rgba(0,102,204,0.2) 85%, 
      rgba(0,0,0,0) 100%
    );
    left: 0;
    right: 0;
  }
  
  .nav-border::before {
    content: '';
    position: absolute;
    top: 0;
    left: -50%;
    width: 200%;
    height: 1px;
    background: inherit;
  }
  
  .nav-item {
    color: var(--color-text);
    text-decoration: none;
    padding: 8px 16px;
    border-radius: var(--radius-sm);
    font-weight: 500;
    font-size: 1rem;
    border-bottom: 0 !important;
  }
  
  .nav-item:hover {
    background-color: var(--color-grey-light);
    color: var(--color-primary);
    border-bottom: 0 !important;
    text-decoration: none !important;
  }
  
  @media (max-width: 600px) {
    .main-nav {
      justify-content: center;
    }
  }
</style> 